# Lessons

- All branches, commits, pushes, and deployments must use the repository
  owner's existing identity and ordinary project naming. Do not use `codex/`,
  AI-labelled branch names, AI co-author trailers, or AI-styled commit wording.
- When the user asks to commit and push the reviewed production work from
  `main`, keep it on `main` unless they explicitly request a separate branch.
